name: fenrir
description: "Know where you are. No signal required."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  geolocator: ^14.0.3
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  path_provider: ^2.1.6
  share_plus: ^13.3.0

  # Opens the SMS composer for FR-7.1. SMS is the right channel precisely
  # because it rides the cellular voice network, which is available in places
  # a data connection is not -- the situation this whole product is built for.
  # Already in the tree transitively via share_plus; declared because the app
  # calls it directly.
  url_launcher: ^6.3.1

  # Magnetometer heading for FR-4.3.
  flutter_compass: ^0.8.1

  # SQLite, bundled rather than borrowed from the platform.
  #
  # This replaced the `sqflite` plugin after a device run failed with
  # "no such module: fts5" while 230 tests passed. Android's system SQLite is
  # whatever the vendor shipped, and this Samsung build (Android 16) has FTS5
  # compiled out, so FR-8.1's search could not work at all on it.
  #
  # The deeper problem was that the tests ran against a *different* SQLite from
  # the device: sqflite_common_ffi bundles its own. Using it everywhere makes
  # the engine deterministic across devices and makes the test suite genuinely
  # representative, which is what let the bug through in the first place.
  # The native library is built from source by package:sqlite3 via build hooks.
  sqflite_common_ffi: ^2.3.6

  # UTM and MGRS conversion for FR-2.1. Pure Dart, BSD-3, no network. Chosen
  # over hand-rolling because the Krüger series expansion behind an accurate
  # transverse Mercator projection is genuinely error-prone. Plus Codes are
  # implemented in-repo instead (see lib/src/geo/plus_code.dart): the
  # open_location_code package pins latlong2 ^0.9.0, which cannot resolve
  # against the ^0.10.1 flutter_map needs.
  geobase: ^1.5.0

  # Verifies a downloaded regional pack against its published digest (FR-5.1).
  # A corrupt pack would otherwise open as a valid archive and return no tiles,
  # which looks like a blank map rather than a failed download.
  crypto: ^3.0.6
  convert: ^3.1.2

  # Path joining for the bundled-asset extraction in lib/src/data.
  path: ^1.9.0

  # Annotations only. Declared explicitly so lib/src/geo stays pure Dart and
  # its tests need no Flutter binding, rather than reaching into
  # package:flutter/foundation.dart for @immutable.
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # NFR-4's 60 fps figure has to come from a real profile-mode run on
  # hardware, not a debug build eyeballed by a person. integration_test drives
  # real gestures on a real device and its own binding.watchPerformance
  # collects the engine's actual FrameTiming, summarised into build/raster
  # times comparable against the 16.6 ms budget. See integration_test/perf_test.dart.
  integration_test:
    sdk: flutter

  # Rasterises Natural Earth geometry into basemap tiles in tools/. Build-time
  # only; never shipped in the app.
  image: ^4.9.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Tier 1 offline data, bundled so the app works from first launch with no
  # download and no network. See requirements FR-3.1 / FR-4.1.
  assets:
    - assets/db/places.db
    - assets/map/basemap.mbtiles

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
