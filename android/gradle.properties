org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# This project lives on D: while the pub cache is on C:. Kotlin's incremental
# compiler relativises plugin sources against the project dir, which throws
# "this and base files have different roots" on Windows across drive letters
# and fails the build. Disabling incremental Kotlin compilation avoids it.
# Alternative fix: move PUB_CACHE onto D: so both share a root.
kotlin.incremental=false
